/**
 * Retourne une nouvelle Promesse.
 * @returns {Promise} Une instance de Promise.
 */
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Pour cet exercice, on n'a pas besoin de logique interne spécifique,
    // on retourne juste l'objet Promise.
  });
}
